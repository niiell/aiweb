# Example environment variables for AIWeb Starter
# Copy this to a .env file and edit values as needed.

PORT=3000
REDIS_URL=redis://127.0.0.1:6379
UPLOAD_DIR=uploads

# ASR (automatic speech recognition)
# Options: mock, openai, google
ASR_PROVIDER=mock
# If using OpenAI, set one of these (OPENAI_API_KEY or OPENAI_API_KEY_FILE for Docker secrets)
OPENAI_API_KEY=
OPENAI_API_KEY_FILE=
# Request verbose JSON with timestamps from OpenAI when true
ASR_TIMESTAMPS=false
# Optional ASR language hint (e.g. 'en' or 'id')
ASR_LANGUAGE=

# Translation / TTS
TRANSLATE_TARGET=id
# TTS language code (used by the TTS service if not inferred)
TTS_LANGUAGE=id-ID

# Audio enhancement (denoise) before ASR
ENHANCE=false

# Merge options for overdub step
MERGE_MODE=replace   # 'replace' or 'mix'
BURN_SUBTITLES=false

# SRT subtitle grouping/timing tuning (used by worker.js)
# - SRT_MAX_WORDS: maximum number of words to group into a single subtitle line
# - SRT_MAX_LINE_DURATION: maximum duration (seconds) for a subtitle line
# - SRT_MAX_CHARS: approximate maximum characters per line
# Tweak these to make subtitles shorter/longer or denser. Defaults chosen for general readability.
SRT_MAX_WORDS=7
SRT_MAX_LINE_DURATION=4.0
SRT_MAX_CHARS=80

# Other settings
# REDIS_URL already above; set OPENAI_API_KEY or Google credentials as needed.
